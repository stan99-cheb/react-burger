import styles from "./order-history-page.module.css";

const OrderHistory = () => {
  return (
    <section className={styles.section}>
      Order history page
    </section>
  );
};

export { OrderHistory };
